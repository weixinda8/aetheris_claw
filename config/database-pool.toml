[database]
# PostgreSQL 连接池配置
max_connections = 50
min_connections = 10
connect_timeout = "30s"
idle_timeout = "10m"
max_lifetime = "30m"
acquire_timeout = "10s"

# 连接测试配置
test_before_acquire = true
test_on_create = true

# 性能优化设置
statement_cache_capacity = 100
